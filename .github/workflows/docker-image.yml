name: Dockerizing to Amazon ECR

on:
  push:       # 트리거
    branches: [ develop-PLOTTING-12 ]
      
env:
  AWS_REGION: ap-northeast-2        # set this to your preferred AWS region, e.g. us-west-1
  ECR_REPOSITORY: plotting            # set this to your Amazon ECR repository name
  

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: production

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: AKIAYS2NTDPZQ7HR73GA
        aws-secret-access-key: 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
        aws-region: ${{ env.AWS_REGION }}

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Build, tag, and push image to Amazon ECR
      id: build-image
      env:
        ECR_REGISTRY: public.ecr.aws/o5s3g1r4/plotting-api:latest
        IMAGE_TAG: ${{ github.sha }}
      run: |
        # Build a docker container and
        # push it to ECR so that it can
        # be deployed to ECS.
        docker  build --platform linux/amd64 --tag 590183865331.dkr.ecr.ap-northeast-2.amazonaws.com/plotting-api:latest .
        docker push 590183865331.dkr.ecr.ap-northeast-2.amazonaws.com/plotting-api:latest
        echo "::set-output name=image::$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"
